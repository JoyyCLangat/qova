description: Run all test suites
---
Run all tests:
1. `cd contracts && forge test -vvv`
2. `bun run test` (turborepo runs vitest across sdk, api, dashboard)
Report coverage numbers and any failures.
